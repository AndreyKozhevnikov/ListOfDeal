<UserControl x:Class="ListOfDeal.Views.WunderListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ListOfDeal.Views"
             mc:Ignorable="d" 
             d:DesignHeight="1300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="4*"/>
        </Grid.RowDefinitions>
        <StackPanel >
            <Button Content="Create processor" Command="{Binding CreateProcessorCommand}"/>
            <Button Content="Create tasks" Command="{Binding CreateTasksCommand}"/>
            <Button Content="Handle completed WLTasks" Command="{Binding HandleCompletedWlTasksCommand}"/>
            <Button Content="Handle completed Actions" Command="{Binding HandleCompletedActionsCommand}"/>
            <Button Content="Handle changed LODActions" Command="{Binding HandleChangedActionsCommand}"/>
            <Button Content="Handle change WLTask" Command="{Binding HandleChangedWLTaskCommand}"/>
            <Button Content="change base" Command="{Binding TestCommand}"/>
        </StackPanel>
        <ListBox ItemsSource="{Binding Logs}" Grid.Row="1" Loaded="ListBox_Loaded" x:Name="listBox1" />
    </Grid>
</UserControl>
