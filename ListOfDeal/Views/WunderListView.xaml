<UserControl x:Class="ListOfDeal.Views.WunderListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ListOfDeal.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel >
            <Button Content="Get Data" Command="{Binding GetActionsCommand}"/>
            <Button Content="Create tasks" Command="{Binding CreateTasksCommand}"/>
            <Button Content="Handle completed actions" Command="{Binding HandleCompletedActions}"/>
        </StackPanel>
    </Grid>
</UserControl>
